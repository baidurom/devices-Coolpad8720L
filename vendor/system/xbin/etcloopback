if [ "$#" == "0" ]; then
    echo "Usage: etcloopback f3|f4 n=<number of iterations> [s=<number of bytes per iteration>]"
else
	am start -a "$* g=4" -n com.broadcom.apps.testframework/.SecElementTesterActivity
fi

